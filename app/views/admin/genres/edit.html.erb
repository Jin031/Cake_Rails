<div class="container">
 <div class="row">
  <div class="col-md-12">
   <h4 class= "mt-3 mb-5">ジャンル編集</h4>

 <%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>

<table class= "my-5">
    <thead>
     <tr>
      <th></th>
      <th></th>
     </tr>
    </thead>
     <tbody>
      <tr>
       <th>ジャンル名 </th>
       <th class= "px-5"><%= f.text_field :name %></th>
       <th><%= f.submit '変更を保存',class:"btn btn-success" %></th>
      </tr>
     </tbody>
   </table>
     <% end %>
     </div>
 </div>
</div>
